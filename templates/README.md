# Templates técnicos

- `fastapi-api-service-template`: APIs de domínio em camadas;
- `fastapi-orchestrator-service-template`: coordenação futura de workflows e workers;
- `python-worker-template`: processamento sem servidor HTTP.

Use os scripts em `tools/` para materializar um componente. Não copie e edite manualmente os templates sem necessidade.
