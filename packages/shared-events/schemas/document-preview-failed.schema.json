{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cci.local/schemas/events/document-preview-failed.schema.json",
  "title": "DocumentPreviewFailed",
  "allOf": [
    { "$ref": "event-envelope.schema.json" },
    {
      "properties": {
        "event_type": { "const": "DocumentPreviewFailed" },
        "payload": {
          "type": "object",
          "required": ["document_id", "client_id", "competence_id", "file_format", "preview_id", "parsing_job_id", "error_message"],
          "properties": {
            "document_id": { "type": "string" },
            "client_id": { "type": "string" },
            "competence_id": { "type": "string" },
            "file_format": { "type": "string" },
            "preview_id": { "type": "string" },
            "parsing_job_id": { "type": "string" },
            "error_message": { "type": "string" }
          },
          "additionalProperties": true
        }
      }
    }
  ]
}
