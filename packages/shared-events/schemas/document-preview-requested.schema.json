{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cci.local/schemas/events/document-preview-requested.schema.json",
  "title": "DocumentPreviewRequested",
  "allOf": [
    { "$ref": "event-envelope.schema.json" },
    {
      "properties": {
        "event_type": { "const": "DocumentPreviewRequested" },
        "payload": {
          "type": "object",
          "required": ["document_id", "client_id", "competence_id", "file_format", "preview_id", "parsing_job_id"],
          "properties": {
            "document_id": { "type": "string" },
            "client_id": { "type": "string" },
            "competence_id": { "type": "string" },
            "file_format": { "type": "string" },
            "preview_id": { "type": "string" },
            "parsing_job_id": { "type": "string" }
          },
          "additionalProperties": true
        }
      }
    }
  ]
}
