{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cci.local/schemas/events/extraction-failed.schema.json",
  "title": "ExtractionFailed",
  "allOf": [
    { "$ref": "event-envelope.schema.json" },
    {
      "properties": {
        "event_type": { "const": "ExtractionFailed" },
        "payload": {
          "type": "object",
          "required": ["extraction_job_id", "document_id", "client_id", "competence_id", "template_id", "template_version_id", "error_code", "error_message"],
          "properties": {
            "extraction_job_id": { "type": "string" },
            "document_id": { "type": "string" },
            "client_id": { "type": "string" },
            "competence_id": { "type": "string" },
            "template_id": { "type": "string" },
            "template_version_id": { "type": "string" },
            "error_code": { "type": "string" },
            "error_message": { "type": "string" }
          },
          "additionalProperties": false
        }
      }
    }
  ]
}
