"use client";
import { KeyRound, ShieldCheck, UserRound } from "lucide-react";
import { PageHeader } from "@/components/layout/page-header";
import { ThemeSurface } from "@/components/layout/theme-surface";
import { Badge } from "@/components/ui/badge";
import { useAuth } from "@/features/auth/auth-provider";
export default function SettingsPage(){const{user}=useAuth();return <div className="space-y-7"><PageHeader eyebrow="Identidade" title="Configurações da sessão" description="Informações reais retornadas pelo Identity Service por meio do BFF."/><div className="grid gap-4 lg:grid-cols-2"><ThemeSurface className="p-6"><span className="grid size-11 place-items-center rounded-xl border border-primary/15 bg-primary/7 text-primary"><UserRound className="size-5"/></span><h2 className="mt-5 font-semibold text-white">{user?.name}</h2><p className="mt-1 text-sm text-muted">{user?.email}</p><div className="mt-5 flex flex-wrap gap-2">{user?.roles.map(role=><Badge key={role} variant="violet">{role}</Badge>)}</div></ThemeSurface><ThemeSurface className="p-6"><span className="grid size-11 place-items-center rounded-xl border border-success/15 bg-success/7 text-success"><ShieldCheck className="size-5"/></span><h2 className="mt-5 font-semibold text-white">Sessão protegida</h2><p className="mt-2 text-sm leading-6 text-muted">Access token mantido apenas em memória. O refresh permanece em cookie HttpOnly administrado pelo BFF.</p><div className="mt-5 flex items-center gap-2 border-t border-border pt-4 text-xs text-muted"><KeyRound className="size-3.5"/> {user?.permissions.length ?? 0} permissões carregadas</div></ThemeSurface></div></div>}
