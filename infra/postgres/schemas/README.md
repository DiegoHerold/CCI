# Schemas por domínio

Schemas canônicos: `identity`, `client`, `document`, `template`, `extraction`, `rule`, `conference` e `report`.

Cada serviço possui suas tabelas e migrations. `auth`, `core` e outros schemas históricos permanecem compatíveis até uma migração explícita; não mover tabelas automaticamente.
